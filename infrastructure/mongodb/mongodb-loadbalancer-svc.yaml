apiVersion: v1
kind: Service

metadata:
  name: mongodb-loadbalancer-svc
  namespace: mail-server

spec:
  type: LoadBalancer
  selector:
    app: mongodb
  ports:
    - port: 27017
      protocol: TCP
      targetPort: 27017
      nodePort: 32000

