apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: redis-pvc
  namespace: mail-server

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
