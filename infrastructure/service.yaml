apiVersion: v1
kind: Service

metadata:
  name: mail-server
  namespace: mail-server

spec:
  type: LoadBalancer
  selector:
    app: mail-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
